extends base.pug

block content

  h1 #{objInCart.name}

  h2 #{objInCart.price}

  a(href=`/items/${objInCart._id}/edit`) edit

  form(action=`/items/${objInCart._id}?_method=DELETE`, method="POST")
    input(type="submit", value="DELETE")
