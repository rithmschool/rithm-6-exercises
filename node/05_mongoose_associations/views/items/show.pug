extends base.pug

block content
    h2 #{item.name}
    a Price = #{item.price}
    br
    a Quantity = #{item.quantity}
    br
    a(href=`/items/${item.id}/edit`) Edit #{item.name}
    br
    form(action=`/items/${item.id}?_method=DELETE`, method="POST")
        input(type="submit" value="X")