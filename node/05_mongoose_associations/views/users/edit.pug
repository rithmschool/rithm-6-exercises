extends ../base.pug
block content
  h1 Edit this user
  form(action=`/users/${foundUser.id}?_method=PATCH` method = "POST")
    input(type="text", name = "name", value = `${foundUser.firstName}`)
    
    input(type="submit" value = "Edit") 
  