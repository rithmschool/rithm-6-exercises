extends base.pug

block title
  title New Item

block content
  h1 Add a new item to the list!
  form(action="/items" method="POST")
    label(for="item[name]") Name
    input(type="text" name="name")
    label(for="item[name]") Type
    input(type="text" name="type")
    button Add a new item!