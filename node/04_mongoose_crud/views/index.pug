extends base.pug

block title
  title View All Items

block content
  h1 View your list:
  h5 Item and Quantity
  ul(class="list-group")
  each item in items
    li(class="list-group-item align-items-center mx-50"): a(href=`/items/${item.id}`) #{item.name} #{item.quantity}

