let name = "Josie"`When ${name} comes home, so good`;

const DO_NOT_CHANGE = 42;

let arr = ([1, 2][
  //
  (2, 1)
] = arr);

//arrow
const double = arr => arr.map(val => val * 2);

//deconstruction

var { a, b } = obj.numbers;

// default params

const add = (a = 10, b = 10) => a + b;

`Array.from` - The Array.from() method creates a new Array instance from an array-like or iterable object.

`Object.assign` - method is used to copy the values of all enumerable own properties from one or more source objects to a target object. It will return the target object.

`Array.includes` - method determines whether an array includes a certain element, returning true or false as appropriate.

`String.startsWith` - method determines whether a string begins with the characters of a specified string, returning true or false as appropriate.
